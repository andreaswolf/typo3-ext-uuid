CREATE TABLE tx_testextension_without_uuid (
    some_input_field VARCHAR(200) DEFAULT '' NOT NULL,
);

CREATE TABLE tx_testextension_with_uuid (
    some_input_field VARCHAR(200) DEFAULT '' NOT NULL,
);

CREATE TABLE tx_testextension_with_uuid_in_tca_ctrl (
    some_input_field VARCHAR(200) DEFAULT '' NOT NULL,
);